# Local OpenSearch connection settings.
# Usage:
#   source local.env
#   docker compose up -d
#   mvn spring-boot:run
#
# Or: make up && make run

export OPENSEARCH_URI="https://localhost:443"
export OPENSEARCH_USERNAME="admin"
export OPENSEARCH_PASSWORD="Str0ng!Demo#9"
export OPENSEARCH_TRUST_SELF_SIGNED="true"
